# Can be an absolute, or relative to current working directory
working_path: spec/var/lib/poste

# Can be absolute, or relative to working path
spool:
  path: spool

# Can be absolute, or relative to working path
tmp_path: tmp

# Can be absolute, or relative to working_path
log_path: log

mongo:
  host: 127.0.0.1
  port:
  user:
  password:
  database: poste

smtp:
  listen:
    - 127.0.0.1:2525
    - 127.0.0.1:2552

relay:
  default:
    - 127.0.0.1:25
